<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme (applied after splash) -->
    <style name="Theme.StudentZK" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme — activity starts with this, then hands off to Theme.StudentZK -->
    <style name="Theme.StudentZK.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.StudentZK</item>
    </style>

</resources>
